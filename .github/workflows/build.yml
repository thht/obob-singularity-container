on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container: docker://quay.io/singularity/singularity:v3.9.4
    steps:
      - name: Build it
        run: echo "Hello"
